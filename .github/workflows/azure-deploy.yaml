name: Azure Deployment

on:
  push:
    branches:
      - master

jobs:
  deploy-backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'
      - name: Install Backend Dependencies
        run: npm ci
        working-directory: ./backend
      - name: Deploy Backend to Azure
        uses: azure/webapps-deploy@v2
        with:
          app-name: my-react-app-backend
          slot-name: production
          publish-profile: ${{ secrets.AZURE_BACKEND_PUBLISH_PROFILE }}
          package: ./backend

  # deploy-frontend:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v3
  #     - name: Set up Node.js
  #       uses: actions/setup-node@v3
  #       with:
  #         node-version: '20'
  #     - name: Install Frontend Dependencies
  #       run: npm ci
  #       working-directory: ./frontend
  #     - name: Build Frontend
  #       run: npm run build && cp ./public/web.config ./build/
  #       working-directory: ./frontend
  #       env:
  #         REACT_APP_API_URL: https://my-react-app-backend-f5eredcae7fababt.canadacentral-01.azurewebsites.net/api/data
  #     - name: Verify Build Output
  #       run: ls -la ./frontend/build
  #       working-directory: .
  #     - name: Zip Build Folder
  #       run: cd frontend/build && zip -r ../../build.zip .
  #       working-directory: .
  #     - name: List Zip Contents
  #       run: unzip -l build.zip
  #       working-directory: .
  #     - name: Deploy Frontend to Azure
  #       uses: azure/webapps-deploy@v3
  #       with:
  #         app-name: my-react-app-frontend
  #         publish-profile:  secrets.AZURE_FRONTEND_PUBLISH_PROFILE }}
  #         package: ./build.zip
  #         clean: true  # Force clean deployment
  #       env:
  #         ACTIONS_STEP_DEBUG: true
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Build Frontend
      run: |
        cd frontend
        npm install
        npm run build

    - name: Deploy Frontend to Azure
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'my-react-app-frontend'
        slot-name: 'production'
        publish-profile: ${{ secrets.AZURE_FRONTEND_PUBLISH_PROFILE }}